CREATE TABLE IF NOT EXISTS ANYTHING.USER (
	SEQ INT NOT NULL AUTO_INCREMENT,
	USER_ID VARCHAR(100) NOT NULL UNIQUE,
	USER_PW VARCHAR(200) NOT NULL,
	USER_NAME VARCHAR(20),
	USER_MOBILE VARCHAR(20),

	PRIMARY KEY(SEQ)
);
